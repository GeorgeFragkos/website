export const scrollToAboutSection = () => {
  window.scrollTo({
    top: 100,
    behavior: "smooth",
  });
};

export const scrollToPortfolioSection = () => {
  window.scrollTo({
    top: 200,
    behavior: "smooth",
  });
};

export const scrollToContactSection = () => {
  window.scrollTo({
    top: 400,
    behavior: "smooth",
  });
};
